from setuptools import setup, find_packages
# this is setup for package src that I have in my project:
# the real name is not src but 'enigma-transformed'

setup(
    name='src',
    version='0.1',
    packages=find_packages(),
)
